server:
  port: 9095

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/04adarsh/microservice-config-server
          clone-on-start: true

